  
on:
  branches:
    - master
  pull-requests: true
services:
  proxy:
    image: nginx
    port: 80
  serve:
    - echo "serve steps for nginx proxy"
    - ls -la
  build:
    - echo "build steps for nginx proxy"
    - ls -la